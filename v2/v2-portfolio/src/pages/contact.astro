---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';

import { Button } from "@/components/ui/button"
import { Form } from "@/components/form"
import '@/styles/globals.css'
import { NavBar } from '@/components/NavBar';
import AnimatedPinCard from "@/components/3d-pin";
import TimelineDemo from "@/components/timeline";
import { Image } from 'astro:assets';

import { supabase } from '@/db/supabase';
const { data, error } = await supabase.from("todos").select('*');
---
<script is:inline>
	const getThemePreference = () => {
		if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
			return localStorage.getItem('theme');
		}
		return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
	};
	const isDark = getThemePreference() === 'dark';
	document.documentElement.classList[isDark ? 'add' : 'remove']('dark');
 
	if (typeof localStorage !== 'undefined') {
		const observer = new MutationObserver(() => {
			const isDark = document.documentElement.classList.contains('dark');
			localStorage.setItem('theme', isDark ? 'dark' : 'light');
		});
		observer.observe(document.documentElement, { attributes: true, attributeFilter: ['class'] });
	}
</script>




<Layout title="ryan's contact">
	<main>
		<div className="bg-zinc-950 dark:bg-white p-4">
		<NavBar client:load />
		</div>

        <body className="flex flex-col items-center gap-4">
            <h1>Contact <span class="text-gradient">Me</span>?</h1>
            <Form client:load />
			<ul role="list" class="link-card-grid" >
				<AnimatedPinCard  client:load />
			</ul>
        </body>
	</main>
</Layout>


