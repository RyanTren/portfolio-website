---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';

import '@/styles/globals.css'
import '@radix-ui/themes/styles.css';
import { Theme } from '@radix-ui/themes';

import AnimatedPinCard from "@/components/3d-pin";
import { MacbookScrollDemo } from "@/components/macbook-feature";
import { NavBar } from "@/components/NavBar";
import { HeroComponent } from "@/components/hero";
import { SVGMaskEffect } from "@/components/mask";
import { LayoutGrid } from "@/components/ui/layout-grid";

import { Image } from 'astro:assets';


import { ViewTransitions } from 'astro:transitions';


const cards = [
	{ id: 1, content: "Card 1", className: "class-1", thumbnail: "/path/to/image1.jpg" },
	{ id: 2, content: "Card 2", className: "class-2", thumbnail: "/path/to/image2.jpg" },
	{ id: 3, content: "Card 3", className: "class-3", thumbnail: "/path/to/image3.jpg" },
	{ id: 4, content: "Card 4", className: "class-4", thumbnail: "/path/to/image4.jpg" },
	{ id: 5, content: "Card 5", className: "class-5", thumbnail: "/path/to/image5.jpg" },
];

---
<script is:inline>
	const getThemePreference = () => {
		if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
			return localStorage.getItem('theme');
		}
		return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
	};
	const isDark = getThemePreference() === 'dark';
	document.documentElement.classList[isDark ? 'add' : 'remove']('dark');
 
	if (typeof localStorage !== 'undefined') {
		const observer = new MutationObserver(() => {
			const isDark = document.documentElement.classList.contains('dark');
			localStorage.setItem('theme', isDark ? 'dark' : 'light');
		});
		observer.observe(document.documentElement, { attributes: true, attributeFilter: ['class'] });
	}
</script>


<Theme>
	<Layout title="ryan's homepage" className="bg-zinc-950 dark:bg-white p-4">
		<main>
			<head className="z-999"><NavBar client:idle/></head>
			
			
			<body className="z-0 h-96 relative w-full overflow-hidden bg-slate-900 flex flex-col items-center justify-center rounded-lg">
				<div transition:name="hero">
					<HeroComponent client:idle={{timeout: 350}}/>
				</div>

				<div>
					<SVGMaskEffect client:visible/>				
					<MacbookScrollDemo client:load/>
				</div>

				<div>
					<LayoutGrid  cards={cards} client:visible/>
				</div>

				<div>
				<ul role="list" class="link-card-grid">
					<Card
						href="/resume"
						title="Ryan's Resume"
						body="Here's my resume!"
					/>
					<Card
						href="/projects"
						title="Ryan's Projects"
						body="All of my projects over the years"
					/>
				</ul>
				</div>

				<div>
				<ul role="list" class="link-card-grid" >
					<AnimatedPinCard  client:visible />
				</ul>
				</div>
			</body>
		</main>
	</Layout>
</Theme>

